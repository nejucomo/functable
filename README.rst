===========
 functable
===========

A small utility for grouping functions/methods into a table. The best documentation is the module docstring:

.. code:: python

    >>> import funtable
    >>> help(functable)

The complete source follows:

.. literalinclude:: functable.py
   :language: python
   :linenos:

